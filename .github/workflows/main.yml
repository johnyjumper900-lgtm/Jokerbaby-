name: Build Full iOS IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build-ios:
    runs-on: macos-14

    steps:
      ########################################################
      # CHECKOUT
      ########################################################
      - name: Checkout repository
        uses: actions/checkout@v4

      ########################################################
      # NODE + BUN
      ########################################################
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      ########################################################
      # INSTALL
      ########################################################
      - name: Install dependencies
        run: bun install

      ########################################################
      # BUILD VITE
      ########################################################
      - name: Build web app
        run: bun run build

      ########################################################
      # DETECT BUILD FOLDER
      ########################################################
      - name: Detect build folder
        id: detect
        run: |
          if [ -f "dist/index.html" ]; then
            echo "web_dir=dist" >> $GITHUB_OUTPUT
          elif [ -f "dist/client/index.html" ]; then
            echo "web_dir=dist/client" >> $GITHUB_OUTPUT
          else
            echo "❌ Build introuvable"
            exit 1
          fi

      ########################################################
      # CAPACITOR CONFIG
      ########################################################
      - name: Generate Capacitor config
        run: |
          cat > capacitor.config.ts <<EOF
          import type { CapacitorConfig } from '@capacitor/cli';

          const config: CapacitorConfig = {
            appId: 'com.jokerbaby.app',
            appName: 'Jokerbaby',
            webDir: '${{ steps.detect.outputs.web_dir }}',
            bundledWebRuntime: false
          };

          export default config;
          EOF

      ########################################################
      # ADD IOS
      ########################################################
      - name: Add iOS platform
        run: |
          rm -rf ios
          npx cap add ios

      ########################################################
      # SYNC CAPACITOR
      ########################################################
      - name: Capacitor sync
        run: npx cap sync ios

      ########################################################
      # CLEAN PUBLIC
      ########################################################
      - name: Clean iOS public folder
        run: |
          rm -rf ios/App/App/public/*
          mkdir -p ios/App/App/public

      ########################################################
      # COPY WEB BUILD
      ########################################################
      - name: Copy web build
        run: |
          echo "📦 Copie du build Vite..."
          cp -R ${{ steps.detect.outputs.web_dir }}/. ios/App/App/public/

      ########################################################
      # COPY PUBLIC ASSETS
      ########################################################
      - name: Copy public assets
        run: |
          if [ -d "public" ]; then
            echo "📁 Copie dossier public..."
            rsync -av public/ ios/App/App/public/
          fi

      ########################################################
      # FORCE COPY ENTIRE PROJECT
      ########################################################
      - name: Force copy full project
        run: |
          echo "📦 Copie complète du projet..."

          mkdir -p ios/App/App/public/fullproject

          rsync -av ./ ios/App/App/public/fullproject/ \
            --exclude ios \
            --exclude node_modules \
            --exclude .git \
            --exclude dist \
            --exclude .next \
            --exclude coverage

      ########################################################
      # VERIFY FILES
      ########################################################
      - name: Verify copied files
        run: |
          echo "📁 Public folder :"
          ls -lah ios/App/App/public

          echo "📁 Full project size :"
          du -sh ios/App/App/public/fullproject

          if [ ! -f "ios/App/App/public/index.html" ]; then
            echo "❌ index.html manquant"
            exit 1
          fi

      ########################################################
      # BUILD IOS APP
      ########################################################
      - name: Build iOS app
        run: |
          cd ios

          rm -rf ~/Library/Developer/Xcode/DerivedData

          xcodebuild \
            -workspace App/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -archivePath App.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            archive

      ########################################################
      # CREATE IPA
      ########################################################
      - name: Package IPA
        run: |
          cd ios

          mkdir Payload

          APP_PATH=$(find App.xcarchive -name "*.app" | head -1)

          if [ -z "$APP_PATH" ]; then
            echo "❌ App.app introuvable"
            exit 1
          fi

          cp -R "$APP_PATH" Payload/App.app

          ditto -c -k --sequesterRsrc --keepParent Payload ../Jokerbaby-Full.ipa

      ########################################################
      # IPA SIZE
      ########################################################
      - name: Show IPA size
        run: |
          ls -lh Jokerbaby-Full.ipa

      ########################################################
      # UPLOAD IPA
      ########################################################
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: jokerbaby-ios-full
          path: Jokerbaby-Full.ipa
          retention-days: 7